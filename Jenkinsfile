pipeline {
    agent any 
    stages {
        stage('Ansible') { 
            steps {
        # sh 'ansible-playbook -i hosts /etc/ansible/playbooks/playbooks.yml(
          sh 'root@167.86.76.204'
       // 
            }
        }
        }
    
}
